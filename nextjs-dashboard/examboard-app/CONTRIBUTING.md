# راهنمای مشارکت در پروژه

از مشارکت شما در این پروژه استقبال می‌کنیم! این سند راهنمای نحوه مشارکت در پروژه را توضیح می‌دهد.

## کد رفتار (Code of Conduct)

با مشارکت در این پروژه، شما موافقت می‌کنید که:
- با احترام با دیگران رفتار کنید
- نظرات سازنده ارائه دهید
- انتقادات را به صورت حرفه‌ای مطرح کنید

## نحوه مشارکت

### گزارش باگ

اگر باگی پیدا کردید:

1. ابتدا بررسی کنید که آیا این باگ قبلاً گزارش شده است
2. اگر گزارش نشده، یک issue جدید ایجاد کنید
3. در issue شامل کنید:
   - توضیح واضح از مشکل
   - مراحل بازتولید مشکل
   - رفتار مورد انتظار
   - رفتار فعلی
   - Screenshots (در صورت امکان)
   - اطلاعات محیط (OS, Browser, Node version)

### پیشنهاد ویژگی جدید

برای پیشنهاد ویژگی جدید:

1. یک issue با label "enhancement" ایجاد کنید
2. توضیح دهید که چرا این ویژگی مفید است
3. در صورت امکان، مثال‌هایی از استفاده ارائه دهید

### ارسال Pull Request

1. **Fork کردن repository**
   ```bash
   git clone https://github.com/your-username/learn-nextjs.git
   cd learn-nextjs/nextjs-dashboard
   ```

2. **ایجاد branch جدید**
   ```bash
   git checkout -b feature/your-feature-name
   # یا
   git checkout -b fix/your-bug-fix
   ```

3. **تغییرات را اعمال کنید**
   - کد را بنویسید
   - تست کنید
   - مطمئن شوید که lint errors ندارید

4. **Commit کردن تغییرات**
   ```bash
   git add .
   git commit -m "feat: اضافه کردن ویژگی جدید"
   ```

   **نکته**: از [Conventional Commits](https://www.conventionalcommits.org/) استفاده کنید:
   - `feat:` برای ویژگی جدید
   - `fix:` برای رفع باگ
   - `docs:` برای تغییرات مستندات
   - `style:` برای تغییرات فرمت
   - `refactor:` برای refactoring
   - `test:` برای تست
   - `chore:` برای تغییرات دیگر

5. **Push کردن به repository**
   ```bash
   git push origin feature/your-feature-name
   ```

6. **ایجاد Pull Request**
   - به repository اصلی بروید
   - روی "New Pull Request" کلیک کنید
   - branch خود را انتخاب کنید
   - توضیحات کامل ارائه دهید

## استانداردهای کد

### TypeScript

- از TypeScript استفاده کنید
- Type safety را رعایت کنید
- از `any` استفاده نکنید (مگر در موارد ضروری)

### Code Style

- از ESLint و Prettier استفاده کنید
- از naming conventions استاندارد استفاده کنید:
  - Components: PascalCase (`ExamsPage`)
  - Functions: camelCase (`handleSubmit`)
  - Constants: UPPER_SNAKE_CASE (`API_BASE_URL`)
  - Files: kebab-case (`exams-page.tsx`)

### کامنت‌ها

- کد خود را document کنید
- از کامنت‌های واضح و مفید استفاده کنید
- از کامنت‌های واضح استفاده نکنید (self-documenting code)

### Testing

- در صورت امکان، تست بنویسید
- تست‌های خود را اجرا کنید
- مطمئن شوید که تست‌های موجود شکسته نشده‌اند

## ساختار کد

### Components

```typescript
// مثال: exams-page.tsx
'use client';

import { useState, useEffect } from 'react';
// imports...

export default function ExamsPage() {
  // State
  const [exams, setExams] = useState<Exam[]>([]);
  
  // Effects
  useEffect(() => {
    // ...
  }, []);
  
  // Handlers
  const handleSubmit = async () => {
    // ...
  };
  
  // Render
  return (
    // JSX...
  );
}
```

### Services

```typescript
// مثال: exams-service.ts
const API_BASE_URL = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:8000';
const EXAMS_API = `${API_BASE_URL}/api/exams/`;

export async function getExams(termId?: number): Promise<Exam[]> {
  // Implementation...
}
```

## Checklist قبل از PR

- [ ] کد با ESLint و Prettier سازگار است
- [ ] TypeScript errors ندارید
- [ ] تست‌ها (در صورت وجود) pass می‌شوند
- [ ] مستندات به‌روز شده است (در صورت نیاز)
- [ ] CHANGELOG.md به‌روز شده است (در صورت نیاز)
- [ ] کد review شده است
- [ ] Commit messages واضح و مفید هستند

## سوالات

اگر سوالی دارید، می‌توانید:
- یک issue ایجاد کنید
- با maintainers تماس بگیرید

## قدردانی

از مشارکت شما در بهبود این پروژه متشکریم! 🙏

