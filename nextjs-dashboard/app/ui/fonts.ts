// Minimal font placeholders so components that import these don't fail during dev.
// In the real project you may replace this with `next/font` usage.
export const inter = { className: 'font-sans' };
export const lusitana = { className: 'font-serif' };
